# AGENTS.md

## 作業ルール
- パッケージマネージャーは **pnpm** を使うこと。
- 変更後は **ビルド** と **テスト** を実行し、どちらも通すこと。
  - テストは非 watch の `pnpm test:run` を使うこと。`pnpm test -- --run` は watch モードに入ることがあるので使わないこと。
  - ビルドは `pnpm build` を使うこと。
- コミットメッセージと Pull Request は **完全に英語** で書くこと。
- 他の作業者の未コミット変更がある前提で作業すること。コミットや staging を行う場合は、自分が編集したファイルだけを対象にし、無関係な変更を revert/stage しないこと。
- X シェア本文は Tier 内容を直接展開せず、基本的に導入文・ハッシュタグ・共有 URL のみを含めること。Tier 状態の共有は URL クエリで扱うこと。
